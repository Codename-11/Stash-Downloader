# Python dependencies for Stash Downloader server-side backend
# Install with: pip install -r requirements.txt

# Required
yt-dlp>=2024.01.01       # Video/metadata extraction (primary scraper)
requests>=2.28.0         # HTTP requests for direct file downloads

# Optional - Reddit features
praw>=7.0.0              # Reddit API access (for saved/upvoted post import)

# Optional - Metadata embedding
piexif>=1.1.3            # EXIF metadata embedding for images

# Note: ffmpeg is also needed for video metadata embedding (system package, not pip)
